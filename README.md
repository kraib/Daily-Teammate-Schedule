## Requirements

`Browser that supports fetch`

## Install

Install the node packages via:

`$ yarn install`

## Starting

To start the app:

`$ yarn start`

## Tests

To run tests:

`$ yarn test`

## Lint

To run linting:

`$ yarn lint`
